server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://db:3306/seemoney?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: seemoney
    password: seemoney123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발용 (나중엔 validate or none)
    show-sql: true
    properties:
      hibernate:
        format_sql: true